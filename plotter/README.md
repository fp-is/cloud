# Plotter

```
usage: app.py [-h] [--data-path PATH] [--output-path PATH]

Generate plots for cloudsim.

optional arguments:
  -h, --help          show this help message and exit
  --data-path PATH    location of the input data
  --output-path PATH  output directory
```